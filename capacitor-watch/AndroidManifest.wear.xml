<?xml version="1.0" encoding="utf-8"?>
<!--
  After running `npx cap add android` in capacitor-watch/,
  REPLACE the contents of:
    capacitor-watch/android/app/src/main/AndroidManifest.xml
  with this file.

  Key Wear OS additions:
  - uses-feature android.hardware.type.watch
  - meta-data com.google.android.wearable.standalone = true
  - Removed PHONE_STATE permission (not available on watch)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Wear OS declaration -->
    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <!-- Wear OS: mark as standalone (works without paired phone) -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:name="co.rakshitr.forgefit.watch.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
